name: Release after

on:
  push:
    branches: [main]
    paths:
      - "after/**"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-go@v6
        with:
          go-version: "1.25"
          cache-dependency-path: after/go.sum

      - name: Test
        working-directory: after
        run: go test ./...

      - name: Build
        working-directory: after
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: "0"
        run: |
          EXT=""
          [ "$GOOS" = "windows" ] && EXT=".exe"
          go build \
            -ldflags "-s -w -X main.version=${GITHUB_SHA::8}" \
            -trimpath \
            -o "../dist/claudeafter-${{ matrix.goos }}-${{ matrix.goarch }}${EXT}" \
            .

      - uses: actions/upload-artifact@v5
        with:
          name: claudeafter-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v5
        with:
          path: dist/
          merge-multiple: true

      - name: Update latest release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: after-latest
          name: after latest (${{ github.sha }})
          prerelease: true
          files: dist/**
